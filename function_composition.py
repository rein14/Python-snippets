# import functools
# from typing import Callable

# ComposableFunction = Callable[[float], float]

# def compose(*functions: ComposableFunction) -> ComposableFunction:
#     return functools.reduce(lambda f, g: lambda x)
